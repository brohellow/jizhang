// 后端接口地址
// 说明：现在用的是 HTTP（未备案/未上 HTTPS），开发调试必须勾选
//   「详情 → 本地设置 → 不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书」
// 以后备案 + 上 HTTPS 后，把这里改成 https://你的域名 即可
module.exports = {
  // 注意：域名 20111108.xyz 未备案，腾讯云会拦截其 GET 请求（返回备案提示页）
  // 备案完成前用服务器 IP 直连；备案+HTTPS 后再改回 https://你的域名
  BASE_URL: 'http://124.222.195.163',
};
